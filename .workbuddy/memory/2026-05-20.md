# 2026-05-20 工作日志

## Nantong Linens B2B 外贸网站构建 - 构建修复完成 ✅

### 修复的问题（按顺序）
1. **sanity.config.ts visionTool 引用残留** — 文件中仍包含 `visionTool()` 调用和 import，移除后解决
2. **ProductCard 组件导入错误** — `src/app/products/[slug]/page.tsx` 缺少 ProductCard 导入，且使用了 default import 而非命名导入 (`import { ProductCard }`)
3. **Sanity Client 空 projectId 崩溃** — 重写 `src/lib/sanity.ts`，改用懒加载模式 (`getClient()`) + 兼容性 shim，使项目在未配置 Sanity 凭证时也能正常构建
4. **Contact 页面 Server Component 中使用 onSubmit** — 提取表单为独立 Client Component (`src/components/ContactForm.tsx`)

### 构建结果
- **状态**: ✅ 全部通过 (exit code 0)
- **静态页面**: `/`, `/about`, `/blog`, `/contact`, `/faq`, `/rfq`, `/sitemap.xml`
- **动态页面**: `/products`, `/products/[slug]`, `/blog/[slug]`
- **开发服务器**: 已启动在 http://localhost:3000

### 待用户完成
- [x] ~~注册 sanity.io 并获取 Project ID~~ ✅ (nk89o1k8)
- [x] ~~在 `.env.local` 填入真实 Sanity 凭证~~ ✅
- [x] ~~安装 GitHub CLI 并登录~~ ✅ (fanjiehao886)
- [x] ~~推送代码到 GitHub~~ ✅ https://github.com/fanjiehao886/nantonglinens
- [x] ~~连接 Vercel 部署~~ ✅
- [x] ~~配置 Vercel 环境变量 (Sanity 凭证)~~ ✅
- [ ] 在腾讯云配置 DNS: CNAME `www` → `cname.vercel-dns.com`
- [ ] 在 Vercel 添加自定义域名 www.nantonglinens.com
- [ ] 替换占位符 WhatsApp 号码和邮箱

## 联系方式更新 + 重新部署 ✅ (2026-05-21 00:04)

### 已替换的联系方式
| 项目 | 旧值 | 新值 |
|------|------|------|
| WhatsApp | +86 123 4567 8900 | **+86 15151361119** |
| Email | info@nantonglinens.com | **fanjieboy@gmail.com** |

### 更新的文件（9 个）
contact/page.tsx, layout.tsx, llms.txt, products/[slug]/page.tsx, faq/page.tsx, blog/[slug]/page.tsx, rfq/page.tsx, page.tsx, Footer.tsx

### 域名绑定状态
✅ www.nantonglinens.com 已生效（Vercel 部署日志显示已 Aliased）

## Sanity Studio 部署 + 示例数据推送 ✅ (2026-05-21 00:12)

### 问题
- Sanity CLI 与 Node.js v21.7.1 不兼容（`html-encoding-sniffer` ESM 错误）
- 尝试了全局安装 `sanity`、`sanity@3`、`@sanity/cli@3.4.2` 均失败
- 最终方案：绕过 CLI，直接通过 **Sanity HTTP Mutate API** (`/v1/data/mutate/{dataset}`) 操作数据

### 解决方案
- 编写 `scripts/push-data.js` 脚本，使用 Node 原生 `https` 模块调用 Sanity API
- API 端点：`{projectId}.api.sanity.io/v1/data/mutate/production`
- 方法：POST with `mutations: [{ createOrReplace: doc }]`

### 已推送到 Sanity 的数据（7 条文档）
| # | 文档类型 | 名称 |
|---|----------|------|
| 1 | Product | Premium Hotel Bed Sheet Set - 200TC Cotton (Featured) |
| 2 | Product | Luxury Hotel Pillowcase - 300TC Egyptian Cotton (Featured) |
| 3 | Product | Hotel Duvet Cover - Microfiber with Button Closure (Featured) |
| 4 | Product | Premium Hotel Bath Towel Set - Combed Cotton |
| 5 | Product | Hotel Mattress Topper - Memory Foam Quilted |
| 6 | Product | Banquet Table Linens - Polyester Damask Weave |
| 7 | Post | How to Choose Hotel Linens: Buyer's Guide for 2025 |

### 注意事项
- Sanity Studio（Web UI 内容管理后台）尚未部署，目前无法在 sanity.io 网页上编辑内容
- 数据已通过 API 写入 Content Lake，网站前端可以正常读取和展示
- 如需后续管理内容：需解决 CLI 兼容问题或部署 Sanity Studio 到 Vercel
